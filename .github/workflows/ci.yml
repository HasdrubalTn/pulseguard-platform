name: ci

on:
  push:
    branches: [main, develop]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  build-test-pack:
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: Directory.Packages.props

      - name: Verify Windows PowerShell compatibility
        shell: powershell
        run: ./eng/test-development-secrets.ps1

      - name: Verify Postman collection variables
        shell: powershell
        run: ./eng/test-postman-collection.ps1

      - name: Verify solution
        shell: pwsh
        run: ./eng/verify.ps1 -CollectCoverage

      - name: Pack building blocks
        shell: pwsh
        run: dotnet pack PulseGuard.slnx --configuration Release --no-build --output artifacts/packages

      - name: Upload packages
        uses: actions/upload-artifact@v4
        with:
          name: pulseguard-nuget-packages
          path: artifacts/packages/*.*nupkg
          if-no-files-found: error
